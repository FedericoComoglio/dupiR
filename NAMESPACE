#exportPattern("^[[:alpha:]]+")
importFrom(graphics, plot)
import(methods)
import(plotrix)


exportClass( "Counts" )

exportMethods(
   	getCounts,
   	getFractions,
	plot,
	summary
)

export(
	computePosterior,
	plotPosterior,
	newCounts,
	getPosteriorParam
)